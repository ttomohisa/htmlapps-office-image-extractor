# Components

Reusable UI fragments that follow `htmlapps-template` conventions live here. The production app keeps the confirmation component embedded in `src/index.template.html` so the built artifact stays one HTML file.
